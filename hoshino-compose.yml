version: "3"
services:
  hoshino-env:
    image: hoshino-env:latest
    expose:
      - 8080
    volumes:
      - ./:/bot/
      - ~/.hoshino:/root/.hoshino # 如果你的静态资源不在 .hoshino 目录下，请在下面加一行挂在参数
    command: python3.8 /bot/run.py
    networks:
      - coolq-bridge
networks:
  coolq-bridge:
    external: true